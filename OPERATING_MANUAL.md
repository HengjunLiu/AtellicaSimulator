# Atellica Solution Simulator 操作手册

## 1. 软件概述

Atellica Solution Simulator 是一个精确模拟西门子医疗检验设备 Atellica Solution 核心功能的模拟器。该模拟器能够与实验室自动化系统(LAS)和医院实验室信息系统(LIS)进行数据交互，支持有UI和无UI两种运行模式，适用于系统集成测试和开发环境。

### 1.1 功能特性

- **LAS 通信**：支持与实验室自动化系统(LAS)的 TCP/IP 通讯，采用 uRAP 协议
- **LIS 通信**：支持与医院实验室信息系统(LIS)的 TCP/IP 通讯，采用 ASTM 协议
- **样本处理**：接收标本工单信息，30分钟后自动生成随机结果并发送回 LIS
- **图形界面**：完整的图形用户界面，支持人工操作和监控
- **无UI模式**：命令行/后台运行模式，支持自动执行测试案例
- **日志管理**：每日自动生成新的日志文件，支持日志文件自动轮换

## 2. 系统架构

采用模块化设计，各模块低耦合：

- **core**：核心模拟逻辑，处理设备状态管理和样本处理
- **las**：LAS 通信模块，实现 uRAP 协议服务端
- **lis**：LIS 通信模块，实现 ASTM 协议服务端
- **ui**：用户界面模块，提供图形化操作和监控
- **config**：配置管理模块，处理参数配置和持久化
- **logger**：日志模块，记录系统运行和通信日志
- **tests**：测试模块，包含所有测试文件和测试工具

## 3. 系统要求

- Python 3.8 或更高版本
- 无额外第三方依赖
- 操作系统：Windows、Linux 或 macOS

## 4. 安装和配置

### 4.1 安装步骤

1. 克隆或下载项目代码到本地
2. 确保 Python 环境已正确安装
3. 无需安装额外依赖包

### 4.2 配置文件

系统使用 JSON 格式的配置文件，默认文件名为 `config.json`，包含以下主要配置项：

- **logger**：日志配置
- **las**：LAS 通信配置
- **lis**：LIS 通信配置
- **core**：核心模拟参数
- **test_inventory**：测试项目库存
- **consumable_inventory**：耗材库存

### 4.3 配置示例

```json
{
    "logger": {
        "level": "INFO",
        "console_output": true,
        "file_output": true
    },
    "las": {
        "host": "0.0.0.0",
        "port": 10001
    },
    "lis": {
        "host": "0.0.0.0",
        "port": 10002,
        "result_delay": 1800
    }
}
```

## 5. 运行模式

### 5.1 有UI模式

完整的图形用户界面，支持人工操作和监控：

```bash
python main.py
```

### 5.2 无UI模式

命令行/后台运行模式，支持自动执行测试案例：

```bash
python main.py --no-ui
```

### 5.3 指定配置文件

```bash
python main.py --config my_config.json
```

## 6. 用户界面详解

### 6.1 主界面布局

主界面分为以下几个主要区域：

1. **设备状态区域**：显示模拟器关键运行参数
2. **内容区域**：
   - 左侧：参数配置
   - 中间：手动样本处理
   - 右侧：详细状态显示
3. **日志显示区域**：独立的 LAS 和 LIS 通讯日志显示
4. **底部按钮区域**：快捷操作按钮

### 6.2 设备状态区域

显示以下关键参数：

- 自动化接口状态
- 仪器处理状态
- LIS连接状态
- 接口位置数量
- 在线试管数量
- 已完成试管数量
- 就绪装载状态
- 可返回样本数
- IP0队列长度
- IP1队列长度
- IP0锁定状态
- IP1锁定状态

### 6.3 参数配置区域

支持手动修改以下设备运行参数：

- 自动化接口状态
- 仪器处理状态
- LIS连接状态
- IP0远程控制状态
- IP0锁所有权
- IP1远程控制状态
- IP1锁所有权

### 6.4 手动样本处理区域

用于处理LAS发来的手动样本处理请求，包括：

- 样本处理提示
- 样本ID显示/输入
- 完成按钮

### 6.5 详细状态区域

显示完整的设备状态信息，包括：

- 仪器健康状态详情
- 队列管理详细信息
- 测试项目库存信息
- 耗材库存信息

### 6.6 日志显示区域

分为两个选项卡：

- **LAS日志**：显示与实验室自动化系统的通信日志
- **LIS日志**：显示与医院实验室信息系统的通信日志

## 7. 主要功能操作

### 7.1 启动模拟器

1. 打开命令行终端
2. 进入项目目录
3. 执行命令：`python main.py`
4. 等待模拟器启动完成

### 7.2 修改设备状态

1. 在"参数配置"区域，选择要修改的参数
2. 从下拉菜单中选择新的状态值
3. 点击"应用"按钮
4. 观察设备状态区域的实时更新

### 7.3 查看日志

1. 在"通讯日志"选项卡中查看LAS和LIS通信日志
2. 日志自动滚动到最新条目
3. 点击"清空日志"按钮可清空当前显示的日志

### 7.4 查看在机标本

1. 点击底部"在机标本"按钮
2. 在弹出的窗口中查看当前在机标本列表
3. 关闭窗口返回主界面

### 7.5 编辑库存

1. 点击底部"编辑库存"按钮
2. 在弹出的窗口中选择要编辑的库存类型（测试项目或耗材）
3. 进行添加、更新或删除操作
4. 点击"保存并关闭"按钮保存更改

### 7.6 LIS模拟

1. 点击底部"LIS模拟"按钮
2. 在弹出的窗口中进行LIS相关操作
3. 关闭窗口返回主界面

## 8. 手动样本处理流程

### 8.1 LOAD请求处理

1. 当LAS发送LOAD请求时，中间区域会显示装载提示
2. 提示信息包含："请手工装载IPX的标本 (样本ID: XXXXX)"
3. 圆圈和文字会闪烁提示
4. 执行实际的样本装载操作
5. 点击"完成"按钮确认操作完成
6. 系统会更新在线试管数量和队列状态

### 8.2 UNLOAD请求处理

1. 当LAS发送UNLOAD请求时，中间区域会显示卸载提示
2. 提示信息包含："请手工卸载IPX的标本 (样本ID: XXXXX)"
3. 圆圈和文字会闪烁提示
4. 显示样本ID输入框（可修改样本ID）
5. 执行实际的样本卸载操作
6. 检查并确认样本ID
7. 点击"完成"按钮确认操作完成
8. 系统会更新在线试管数量和队列状态

## 9. 日志管理

### 9.1 日志文件位置

日志文件默认存储在 `logs` 目录下：

- `atellica_simulator.log`：系统运行日志
- `las_communication.log`：LAS 通信日志
- `lis_communication.log`：LIS 通信日志

### 9.2 日志文件命名

每日自动生成新的日志文件，命名格式：`XXXXXXXX-YYYY-MM-DD.log`

### 9.3 日志文件轮换

支持日志文件自动轮换，默认保留7天日志

## 10. 测试功能

### 10.1 测试文件组织

所有测试文件统一存放在 `tests` 目录下，便于管理和维护。

### 10.2 运行测试脚本

#### 运行所有测试

```bash
python -m pytest tests/ -v
```

#### 运行特定测试文件

```bash
python -m pytest tests/test_las.py -v
```

#### 运行特定测试类或方法

```bash
python -m pytest tests/test_las.py::TestLASModule -v
python -m pytest tests/test_las.py::TestLASModule::test_initialization -v
```

### 10.3 测试内容

测试脚本验证系统核心功能，包括：

- 仪器健康状态获取和响应
- 测试项目库存管理
- 耗材库存管理
- 样本接收和处理
- 设备状态更新
- LAS 和 LIS 通信协议实现
- 系统初始化和配置加载

## 11. 故障排除

### 11.1 端口冲突

如果启动时出现端口冲突错误：

1. 检查配置文件中的端口设置
2. 确保指定的端口未被其他服务占用
3. 修改配置文件中的端口号，重新启动模拟器

### 11.2 日志文件问题

如果日志文件无法生成：

1. 检查配置文件中的日志设置
2. 确保 `logs` 目录存在且可写
3. 检查日志级别设置

### 11.3 连接问题

如果LAS或LIS无法连接：

1. 检查配置文件中的主机和端口设置
2. 确保模拟器已成功启动
3. 检查防火墙设置，确保端口已开放
4. 查看通信日志获取详细错误信息

## 12. 注意事项

1. 模拟器仅用于测试和开发环境，不得用于生产环境
2. 确保端口配置不与其他服务冲突
3. 定期清理日志文件，避免占用过多磁盘空间
4. 配置文件中的敏感信息应妥善保管
5. 运行模拟器前确保Python环境已正确安装

## 13. 版本历史

- v1.3.8：当前版本
  - 修复手动处理LOAD标本后在线试管数量不增加的问题
  - 修复IP0队列长度不减少的问题
  - 优化手动处理提示，添加样本ID显示
  - 修复UNLOAD请求处理逻辑

- v1.1.0：增强版本，优化日志系统和用户体验
  - 新增每日自动生成日志文件功能
  - 优化日志文件命名格式
  - 实现日志文件自动轮换
  - 优化UI日志显示
  - 更新.gitignore文件
  - 修复日志显示问题
  - 增强系统稳定性和性能

- v1.0.0：初始版本，实现核心功能
  - 实现LAS和LIS通信协议
  - 支持有UI和无UI两种运行模式
  - 提供完整的样本处理流程
  - 实现设备状态管理和监控

## 14. 联系方式

如有问题或建议，请联系开发团队。

---

**版权所有 © 2026 西门子医疗**
**文档版本：v1.3.8**
**发布日期：2026-01-26**